For a completed coding task, run in order where applicable: `vp run lib-build`, `vp check`, `vp run -r typecheck`, `vp test run`; for build/release changes, inspect `packages/app/dist` output names/content instead of running an artifact-validation command. Repository guidance requires committing progress immediately after each task; commits use signed Conventional/Angular style with Chinese description.