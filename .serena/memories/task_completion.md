For a completed coding task, run in order where applicable: `vp run lib-build`, `vp check`, `vp run -r typecheck`, `vp test run`; for artifact/build changes also run `vp run artifacts` and inspect `packages/app/dist` output names/content. Repository guidance requires committing progress immediately after each task; commits use signed Conventional/Angular style with Chinese description.